apiVersion: v1
kind: Service
metadata:
  name: {{ include "reverse-hash-service.fullname" . }}-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: {{ include "reverse-hash-service.name" . }}-db
